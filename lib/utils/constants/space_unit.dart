class SpaceUnit {
  static const eighthBase = base * 0.125;
  static const quarterBase = base * 0.25;
  static const halfBase = base * 0.5;
  static const threeQuarterBase = base * 0.75;
  static const base = 8.0;
  static const doubleBase = base * 2;
  static const tripleBase = base * 3;
  static const quadrupleBase = base * 4;
}
